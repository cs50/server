server {
    index index.html;
    listen ${port} default_server;
    location / {
        include proxy_params;
        proxy_pass http://unix:/var/run/gunicorn.sock;
    }
    location /static {
        root /var/opt/server50/html;
    }
    server_name localhost;
}
